const { request, response } = require('express');

module.exports = {
    endpoint: 'test',
    method: 'POST',
    /**
     * @param {request} req 
     * @param {response} res 
     */
    run: async (req, res) => {

    }
}